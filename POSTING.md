# 포스팅 작성 가이드 — Anthropic 글 번역·요약

주 워크플로우: [anthropic.com](https://www.anthropic.com/) 의 post / story / announcement / research →
**번역 → 요약 → 본인 해석** 후 발행.

> 코드 변경 없는 **작성 컨벤션** 문서다.
> 출처/참고자료는 별도 기능이 아니라 본문 markdown 안에 약속된 형식으로 적는다.
> (`Post` 타입의 본문은 `bodyMd` 단일 필드 — `src/lib/types.ts`,
> 렌더링은 `src/components/post/MarkdownView.tsx`, `react-markdown + remark-gfm`)

---

## 1. 에디터 입력 — velog식 미니멀

`/admin/editor`(`src/components/admin/PostEditor.tsx`)는 velog처럼 **제목·태그·카테고리·본문** 넷만 입력한다.
나머지는 전부 자동이다.

| 필드 | 어떻게 채워지나 |
|---|---|
| 제목 | 상단 큰 입력칸에 직접. 직역·점잖은 제목 ✗ — 클릭하고 싶게 후킹하는 제목으로 (2-0절) |
| 태그 | 제목 아래 입력칸에 쉼표 구분 (`anthropic, 보안, ai-security` 등). **표기는 저장 시 자동 정렬** — 이미 쓰이는 태그와 대소문자·공백만 다르면 기존 표기로 맞춘다(`canonicalizeTags`). 새 태그는 입력한 표기 그대로 살아남으니 표기를 신중히 고른다 |
| 카테고리 | 작은 드롭다운 (`anthropic` 계열 / 주제 카테고리) |
| 본문 | 마크다운. 이미지는 **드래그·붙여넣기**로 자동 업로드(별도 버튼 없음) |
| ~~슬러그~~ | **자동** — 제목에서 slugify(ASCII 영문/숫자/하이픈만, 한글·기호 제거). 기존 글은 URL 안정성을 위해 원래 슬러그 유지. 한국어 제목은 영문 자동 fallback(`post-<timestamp>`) 으로 떨어지니 인물명·핵심 키워드 슬러그를 쓰려면 `/draft` 직접 경로에서 frontmatter `slug:` 에 명시한다 |
| ~~요약(excerpt)~~ | **자동** — 본문 첫 `>` 블록쿼트에서 파생(`deriveExcerpt`). 카드/검색/SNS/SEO/RSS/OG가 보는 문장과 페이지상 두괄식 훅이 **같은 문장**이다 |
| ~~썸네일 종류~~ | **자동** — 슬러그 해시로 a–f 6변형에 결정적 분포(`thumbKindFromSlug`). 카드 단조로움 방지 |
| ~~읽는 시간~~ | **자동** — 본문 분량에서 산출(`deriveReadingMin`) |
| ~~시리즈 · 추천 옵션~~ | 입력란 없음. 기존 값은 row update에 포함 안 해 보존됨 |

> 본문은 `> 요약` → `## 헤드라인` 순서로 시작한다.
> 맨 위에 `# 제목`을 다시 쓰지 않는다 — 제목은 입력칸이 권위.
> 본문 첫 `>` 인용구는 페이지의 두괄식 훅이면서 동시에 `excerpt` 컬럼에 자동 추출되어 카드·OG·RSS에 동일 문장으로 들어간다.

---

## 2-0. 제목 — 요약이 아니라 클릭을 만드는 한 줄

제목은 그 글에서 **가장 오래 붙들고 고쳐야 하는 한 줄**이다. `<title>` 태그이자 H1·OG 제목이고, 목록·카드·검색 결과에서 독자가 보는 전부다. 본문이 아무리 좋아도 제목에서 안 눌리면 0이다.

**이 글을 쓴 기자라고 생각하고, 그 기사의 헤드라인을 뽑는다.** 내용을 성실하게 압축하는 게 목표가 아니다. **읽고 싶게 만드는 것**이 목표다.

### 원칙 — 요약하지 마라, 틈을 남겨라

제목을 망치는 가장 흔한 실수는 **본문의 결론을 제목에 다 적어 버리는 것**이다. 결론을 알면 클릭할 이유가 사라진다.

제목이 해야 할 일은 **긴장을 만드는 것**이다. 독자 머릿속에 "어? 왜?"가 생기고, 그 답이 본문에만 있어야 한다.

✗ `프롬프트 캐시가 깨지면 토큰 값 50배, Claude Code 2.1.265가 고친 건 비용이었다`
 — 원인·수치·버전·결론이 전부 있다. 다 읽었다. 클릭할 이유가 없다.
○ `토큰 값이 50배 뛰는 지점, Claude Code 2.1.265가 급히 고친 곳`
 — 무엇이 50배인지, 어디를 고쳤는지는 본문에 있다.

✗ `Navier-Stokes를 푼 88시간과 매출 0의 72시간을 가른 건 검증기였습니다`
 — "가른 건 X였다"까지 적으면 본문에 남는 게 없다.
○ `88시간 만에 난제를 푼 에이전트와 72시간 돌리고 매출 0, 둘을 가른 조건`

**한 문장 판별법: 제목을 읽고 나서도 본문에서 알고 싶은 게 남는가?** 남지 않으면 결론을 한 칸 뒤로 물린다.

### 제목을 만드는 네 단계

1. **가장 센 사실 하나를 고른다.** 수치, 반전, 모순, 의외의 결과 — 글에서 제일 강한 것 하나. 두 개 넣으려다 둘 다 약해진다.
2. **검색어를 앞에 박는다.** 독자가 실제로 칠 말(제품명·기능명·사건·개념)이 **앞쪽**에 있어야 한다. 검색 결과에서 제목은 한글 30자 안팎에서 잘린다.
3. **결론을 반 칸 감춘다.** "X 때문이다"를 "~한 이유" / "둘을 가른 조건" / "따로 있던 그 지점"으로 바꿔 본다. 감추면서 동시에 명사로 끊어진다.
4. **소리 내어 읽고, 안 눌릴 것 같으면 버리고 다시 뽑는다.** 후보를 최소 3개 만들어 견준 뒤 고른다. 첫 후보를 그대로 쓰지 않는다.

### 클릭을 만드는 장치 (돌아가며 쓴다)

- **숫자의 낙차** — `23명이 하던 일을 혼자`, `88시간 vs 72시간`. 두 수를 부딪치면 강해진다.
- **부정·반전** — `모델이 아니었던 병목`, `Mathlib이 받지 않은 코드`. 기대를 꺾는 자리에 긴장이 생긴다.
- **모순 병치** — `난제는 풀었는데 매출은 0`. 둘을 나란히 놓고 이유는 적지 않는다.
- **구체적 장면** — `stat-card 하나 없이 4개월`. 추상어보다 장면이 눌린다.
- **직접 인용** — `"코딩은 이미 끝났다"`. 따옴표는 시선을 끈다.
- **미완결 지목** — `따로 있던 진짜 병목`, `거기서 갈리는 지점`. 대상을 가리키되 이름은 본문에.

**낚시는 금지다.** 본문이 답하지 못하는 궁금증은 만들지 않는다. 제목이 약속한 건 본문 안에 반드시 있어야 한다. 틈을 남기는 것과 없는 걸 있는 척하는 건 다르다.

### 하지 말 것

- **인물·회사명을 주어로 앞세우지 않는다.** 인터뷰 대상자·발표자·회사 이름은 제목의 주인공이 아니다 — `##` 소제목이나 발표자 소개 문단에 두면 충분하다. 독자는 사람 이름보다 그 사람이 한 말의 알맹이에 끌려 클릭한다.
  ✗ `gstack — YC 사장 Garry Tan이 Claude Code를 엔지니어링 팀으로 굴리는 법`
  ○ `Claude Code 하나로 굴리는 23명짜리 엔지니어링 팀`
  ✗ `Claude Code 창시자 Boris Cherny — "코딩은 이미 끝났다, 다음은 무엇인가"`
  ○ `"코딩은 이미 끝났다" — 손코딩 0, 하루 PR 150개의 현재`
  예외: 그 인물·회사 자체가 독자가 검색할 핵심어일 만큼 주제와 한 몸일 때.
  **제품·기능·모델 이름은 여기서 말하는 "인물·회사명"이 아니다** — 오히려 검색 통로라 앞에 둔다.
- **제목을 서술형 문장으로 끝내지 않는다 — 명사로 끊는다.**
  `~습니다 / ~입니다`는 물론이고 `~했다 / ~아니었다 / ~않는다` 같은 평서 종결도 제목에는 쓰지 않는다. 본문 말투가 제목까지 넘어오면 문장이 늘어지고 힘이 빠진다.
  **기본형은 명사·체언으로 끊는 것**이다 — `~한 이유`, `~를 가른 조건`, `~가 고친 곳`, `~로 빠져나가는 길`, `~할 때 볼 것`, `~의 줄타기`.
  ✗ `에이전트 도입을 막는 건 모델이 아니라 형상관리입니다`
  ✗ `에이전트 도입을 막는 건 모델이 아니었다` — 종결어미만 바꾼 것도 여전히 서술형 문장이다
  ○ `Ant Apply가 드러낸 진짜 병목`
  ✗ `AI 튜터를 쥐여 줬더니 1만 7천 명이 강의를 그만뒀다`
  ○ `AI 튜터를 쥐여 준 1만 7천 명이 강의를 그만둔 이유`
  **의문문은 예외로 허용한다** — `Claude Plugin Eval은 왜 스킬 점수를 두 번 재나`. 인용 한 토막(`"코딩은 이미 끝났다"`)도 그 자체가 명사 덩어리라 괜찮다.
- **영어는 첫 글자를 대문자로 쓴다.**
  제품·기능·도구 이름은 소문자로 흘리지 않는다 — 제목에서 눈에 걸리는 덩어리여야 한다.
  ✗ `ant apply가 드러낸 병목` / `claude plugin eval의 Δ`
  ○ `Ant Apply가 드러낸 병목` / `Claude Plugin Eval의 Δ`
  본문에서 명령·식별자로 쓸 때는 원래 표기 그대로 `인라인 코드`(`ant apply`)로 적는다 — 이 규칙은 제목에 한한다.
- **플랫폼 고유 용어를 한국어로 옮기지 않는다.**
  ✗ `별 6만 7천 개` — GitHub의 star를 "별"로 옮기면 무슨 말인지 통하지 않는다.
  ○ `GitHub Star 6만 7천`
  같은 부류: `Pull Request`, `Issue`, `Fork`, `Commit`, `Release`, `Changelog`. 한국 개발자가 영어로 부르는 것은 영어로 둔다(2절 "제품 UI의 모드·메뉴·플래그 이름도 영어 원표기").
- **주장만 있고 대상이 없는 제목 ✗.** 검색으로 이 글에 닿을 통로가 사라진다.
  ✗ `모델이 아니었던 진짜 병목` (무엇에 대한 글인지 모른다)
  ○ `Ant Apply가 드러낸 진짜 병목`
- **대상만 있고 주장이 없는 제목 ✗.** 그건 보도자료 헤드라인이다.
  ✗ `Anthropic이 내놓은 Ant Apply`
- 밋밋한 요약, 원문 제목 직역, 내용 없는 낚시 — 셋 다 ✗.

### 제목 형태를 매번 바꾼다 — 한 가지 틀로 찍지 않는다

가장 흔한 자동화 티가 **`키워드 — 부연구`** 한 형태(em-dash로 앞뒤를 가르는 틀)를 매 글에 쓰는 것이다. 직전 글 1~2개의 제목 형태를 보고 **이번엔 다른 골격**을 고른다.

**기본은 "독자가 실제로 입에 올릴 법한 짧은 말"이다.** 검색창에 치거나 동료에게 물을 때 쓰는 문장 그대로. 길이는 한글 20자 안팎, 길어도 30자. (2026-09-16 피드백)

✗ `LLM 이름 뒤에 붙은 7B가 가리키는 76억 개 숫자의 정체` — 정보를 명사구에 눌러 담은 "문학적" 제목. 아무도 이렇게 말하지 않는다.
○ `LLM 모델 뒤에 붙는 B가 뭘까요?` — 독자가 실제로 하는 질문 그대로. 짧고, 검색어(LLM·B)가 앞에 있고, 답은 본문에.
○ `7B 모델에 GPU 메모리 14GB가 필요한 이유` — 같은 원칙의 명사구 판. 짧으면 명사구도 좋다.
○ `모든 문제에 LLM이 필요한 것은 아니다` — 단정 평서도 된다.

**압축하지 않는다.** `~의 정체`·`~하는 지점`·`~가 갈리는 자리`·`둘을 가른 조건` 같은 명사 마무리, `A·B·C` 중점 나열, 수치 두 개 병치는 전부 정보를 눌러 담는 장치라 제목이 무거워진다. 하나만 남기고 나머지는 본문으로 내린다.

**한 골격으로 통일하지 않는다.** 질문형이 잘 읽힌다고 시리즈 전부를 `~까요?` 로 끝내면 그게 자동화 티다(2-3절). 질문형·명사구·단정 평서를 섞는다.

토막 병치(쉼표·em-dash 로 앞뒤를 가르는 틀)는 **한 문장으로 흐르게 만들 수 없을 때만** 쓴다. 최근 글에 병치가 몰려 있으면 자동화 티다.

- **질문형** — `LLM 모델 뒤에 붙는 B가 뭘까요?`, `LangGraph는 왜 토큰을 줄줄 흘리나`. `~죠?` 는 제목에서도 ✗, `~까요?` 로.
- **짧은 명사구** — `7B 모델에 GPU 메모리 14GB가 필요한 이유`, `에이전트 256대를 띄우기 전에 diff 부터 봐야 하는 이유`
- **단정 평서** — `모든 문제에 LLM이 필요한 것은 아니다`, `AI 모델은 배포가 끝이 아니다`
- **명사구 단독** — `컨트롤 플레인과 노드로 보는 쿠버네티스`
- **인용·발언** — `"코딩은 이미 끝났다"`
- **쉼표로 사건 + 명사 결말** — `Claude Code 소스 유출, Anthropic이 택한 다음 수` (보조)
- **수치 앞세움** — `토큰 70배 절감, 그 자리에 있던 것` (보조)
- **두 항 병치** — `Navier-Stokes 88시간과 매출 0의 72시간, 둘을 가른 조건` (보조)
- **em-dash 두 토막** — `Graphify — 코드를 지식 그래프로` (가끔만)

어느 골격이든 **끝은 명사로 떨어진다**(질문형·인용 제외). 종결어미로 끝나면 골격을 다시 잡는다.

**em-dash(` — `)로 앞뒤를 가르는 제목이 두 글 연속이면 이번엔 다른 골격**을 쓴다. 같은 골격뿐 아니라 같은 **시작 단어·같은 어미 패턴**(`~하나`, `~까`, `~다`)이 반복되지 않게 한다. 후킹+검색어 원칙은 골격과 무관하게 지킨다.

### 제목이 정해지면

- 같은 키워드를 excerpt(첫 `>` 인용구)·`##` 헤드라인에도 자연스럽게 반복한다.
- **제목이 남긴 틈은 본문이 반드시 메운다.** 제목에서 감춘 결론이 본문 어디에 있는지 확인하고 넘어간다.

> 제목만으로 검색 상단에 오르지는 않는다.
> 사이트 신뢰도·콘텐츠 깊이·경쟁 강도가 함께 작용한다.
> 경쟁이 덜한 구체적 키워드(롱테일)일수록 작은 블로그도 걸릴 여지가 크다.

## 2. 말투 · 본문 골격

**목적: 요약하고, 담백하게 풀어 쓴다.**
이 블로그는 단순 정보 제공처가 아니라 **개인 기록 블로그**다.
원문을 통째로 옮기지 않는다 — 한 번 소화한 뒤 핵심만 추려, 내 말로 다시 들려준다.
기본값은 "재미"보다 **담백함**이다. 꾸미지 말고 짧고 곧게 쓴다.

**담백하게 — 수식어를 덜어내고 사실을 곧게 놓는다.**
글이 번역체로 부자연스러워지는 가장 흔한 원인은 강조 수식·인과 포장·비유 동사다.
빼도 뜻이 그대로면 뺀다.

- **불필요한 수식어·형용사를 뺀다.**
  "가장 강력한 / 이렇게 센 / 처음으로 / 단적인 / 무려" 같은 강조 수식은 사실에 꼭 필요할 때만 남긴다.
  ✗ "**가장 강력한** Mythos급 모델을 **처음으로** 일반에 공개했습니다."
  ○ "새 모델 Fable 5를 공개했습니다."
- **인과를 포장하지 않는다 — 사실을 일어난 순서대로.**
  "~할 수 있었던 건 ~덕분이죠", "흥미로운 건 ~", "공교롭게도 ~" 같은 도입·포장을 걷어낸다.
  ✗ "이렇게 센 모델을 풀 수 있었던 건 새 안전장치 **덕분이죠** —"
  ○ "Mythos는 보안 문제로 출시가 미뤄졌고, 거기 안전장치를 단 게 Fable 5입니다."
- **비유 동사 대신 실제 동작을 쓴다.**
  "스스로 물러난다 / 빗장을 연다 / 두뇌·일꾼" 같은 비유는 한 글에 한둘이면 양념이지만, 사실 문장에서는 실제 동작으로 적는다.
  ✗ "Claude Opus 4.8로 스스로 **물러납니다**."
  ○ "Claude Opus 4.8로 모델을 바꿔 응답한다고 합니다."
- **한 문장 = 한 사실.**
  한 문장에 사실을 두세 개 욱여넣지 말고 끊는다. 짧은 문장이 모이면 담백해진다.

**사실은 정확하게.**
수치·고유명사·발언(인용) 같은 사실관계는 원문 그대로 둔다.
사실을 지어내거나 왜곡하지 않는다.
인용은 발화자를 원문대로 밝힌다.

**감상·해석은 자유롭게.**
사실을 옮기는 데 그치지 않고, 글쓴이의 반응·해석·평가를 자연스럽게 섞어도 된다.
"의외였다", "이래서 흥미롭다" 같은 개인적인 시선이 곧 개인 블로그의 색이다.
단, 사실과 감상은 독자가 구분할 수 있게 쓴다 — 감상을 사실인 것처럼 끼워 넣지 않는다.

**말투: `-습니다` 뼈대에, 대화형 종결을 양념으로.**
이 블로그의 톤 모델은 보도자료가 아니라 **Claude Chat·ChatGPT·Gemini의 답변체**다.
사실은 단단하게, 말투는 사람이 옆에서 설명하듯 — 그러면서도 **글의 무게는 지킨다**.
사건·공지·기술 글이 일상 블로그처럼 가벼워지지 않게 한다.

- **뼈대는 `-습니다 / -입니다`.**
  문장의 대부분은 격식 종결이다. 핵심 사실·수치·결론은 항상 격식으로 단정한다.
- **양념으로 섞는 종결.**
  - `~까요? / ~일까요?` — 질문을 던져 독자를 끌어들일 때.
  - `~네요` — 가벼운 반응. 드물게.
  - `~거든요` — 배경·이유를 한 박자 풀어 줄 때. 한 글에 1~2회 선에서.
  - `~다고 합니다 / ~라고 합니다` — 출처 있는 사실을 부드럽게 전할 때.
- **피하는 종결.**
  - **`~죠 / ~겠죠` 금지.** 동의를 구하는 말끝이라 AI 챗봇 화법으로 읽힌다. 단정할 곳은 `~습니다`로 단정하고, 풀어야 하면 `~거든요`나 `~다고 합니다`로 바꾼다.
    ✗ "자동화 도구를 500만 번 돌려도 안 잡히던 결함**이죠**."
    ○ "자동화 도구를 500만 번 돌려도 안 잡히던 결함**입니다**."
    제목·서두의 가벼운 질문도 `~죠` 가 아니라 `~까요`로 — "B가 뭘까요?", "예를 들어볼까요?" (2026-09-16 확인).
  - `~해요 / ~예요 / ~잖아요 / ~더라고요` 같은 본격 해요체 — 글의 무게를 떨어뜨린다.
  - `~한 셈입니다 / ~기에 충분합니다 / ~에 다름 아닙니다` 같은 번역서 종결.

비율 감각은 단순하다 — 양념 종결은 **글 전체에서 두세 군데**면 충분하다. 기본은 `~습니다`/`~다고 합니다`로 곧게 간다.
양념이 사실 전달을 늦추거나 글을 구구절절하게 만들면 뺀다.
단정할 곳은 `~습니다`로 단정하고, 흐름이 굳어 보이는 곳만 드물게 풀어 준다.
신문 보도체(사실을 툭툭 떨구는 건조한 톤)는 여전히 피한다.
독자 옆에서 차근차근 풀어 주는 **AI 어시스턴트 답변체**가 기준이다.
짧은 문장과 긴 문장을 섞어 읽는 리듬을 살린다.

한 단락 예시:
> Anthropic이 27년 묵은 OpenBSD 결함을 찾아냈습니다. 자동화 도구를 500만 번 돌려도 안 잡히던 결함입니다. AI의 취약점 발견 능력이 이미 최정상급 전문가 수준에 올라섰다는 뜻입니다.

격식 — 부드러운 격식 — 격식으로 굴린다.
"옆에서 풀어 주는" 톤은 살되, 글의 무게는 그대로다.

**대제목(`##` 헤드라인) = 짧은 명사형 라벨이 기본.**
그 섹션이 다루는 주제를 짧은 명사구로 적는다.
○ `## 코드 유출 원인` / `## Anthropic의 대응` / `## 에이전트 루프` — 그 글만의 주제어가 들어간 라벨.
○ 서술형(`~했다`/명사 종결)도 짧으면 가능 — `## 1억 달러 투입`, `## 빅테크 11곳과 손잡았다`.
✗ `## 무슨 일인가` / `## 왜 중요한가` / `## 어떻게 동작하나` — 그 글만의 주제어가 없는 빈 목차 라벨.
✗ `~했어요/~예요/~거든요` 같은 구어체 — 헤드라인은 본문보다 한 단계 무겁게 잡는다(본문에서 구어를 섞더라도 헤드라인은 명사구·짧은 서술형으로).
✗ **`라벨 — 부연` 처럼 em-dash 로 두 토막을 잇는 헤드라인.** `## 세계 — 오픈소스 비중은 오히려 줄었다`, `## 대책 1 — 리뷰어도 에이전트로`, `## 정리 — 네 층을 겹친다`. 한 글에 몇 개만 쌓여도 목차가 슬라이드 제목처럼 읽힌다(2026-09-16 피드백). 한 덩어리 명사구로 다시 쓴다 — `## 세계 기업에서는 오히려 줄어든 오픈소스 비중`, `## 첫 번째 대책, 리뷰어도 에이전트로`, `## 네 층을 겹쳐야 하는 이유`. `정리 —` 접두도 빼고 정리 내용 자체를 라벨로 쓴다.
(글 제목은 예외 — 2-0절대로 후킹하게.)

**번역체 금지 — 단어가 아니라 한국어로 다시 쓴다.**
영어와 한국어는 어순도 표현도 다르다. 단어만 옮기면 뜻은 맞아도 한국어로는 어색하다.
영어 문장 구조를 그대로 옮긴 아래 패턴을 특히 경계한다 — 한국어 화자가 실제로 쓰는 말로 바꾼다.

- **존재구문 "~가 있습니다"(There is/are) 남발.**
  ✗ "Anthropic이 공개한 실험 연구가 있습니다. 그 연구는 ~"
  ○ "Anthropic이 공개한 실험 연구를 보면, ~" (사실을 동사로 풀어 한 문장에)
  ✗ "그런 일이 셋 있습니다."
  ○ "크게 세 가지가 그렇습니다."
- **추상·무생물 주어 + 영어 동사·관용구 직역.**
  ✗ "모델 회사도 같은 방향을 가리켰습니다." (point in the same direction)
  ○ "모델을 만드는 회사들도 진작부터 같은 말을 해 왔습니다."
  ✗ "여기도 답은 이미 나왔습니다." (the answer is already out)
  ○ "이쪽도 오래갈 기술은 아닙니다."
- **명사화한 술어 "~은 ~입니다"로 영어 정의문(X is Y)을 흉내.**
  ✗ "고를 때 물을 것은 하나입니다." (the thing to ask is one)
  ○ "따질 건 하나뿐입니다."
- **영어 부사를 한 단어로 옮기지 않는다 — 상황을 풀어 쓴다.**
  영어는 부사 하나에 정황을 압축하지만, 한국어는 그 정황을 문장으로 편다.
  부사만 사전에서 바꿔 놓으면 뜻은 통해도 아무도 쓰지 않는 말이 된다.
  특히 **`조용히`(silently)** — 한국어에서 실패·무시·삭제를 꾸미지 않는 말이다.
  ✗ "cron이 몇 달간 **조용히** 실패했습니다." (silently failed)
  ○ "cron이 몇 달간 실패했는데, **어디에도 표시가 나지 않았습니다**."
  ✗ "라이브러리가 스타일을 **조용히** 버렸습니다." (silently dropped)
  ○ "라이브러리가 스타일을 **에러도 경고도 없이** 버렸습니다."
  같은 식으로 걷어낼 부사: `우아하게`(gracefully) → "무너지지 않고 / 하던 일을 마치고",
  `공격적으로`(aggressively) → "최대한 / 되도록 많이", `무겁게`(heavily) → "많이 기대어",
  `단순히`(simply)·`기본적으로`(basically)·`말 그대로`(literally) 남발.
- `~에 가깝습니다`, `~된 생각은 단순합니다` 같은 직역투도 같이 걷어낸다.

**영어 기술 은유를 한국어 단어로 직역하지 않는다 — 뜻으로 풀거나 원어 그대로.**
영어 개발 용어에는 은유가 많다(orphan, dangling, silently, gracefully, stale, hot, heavy, naive).
그 은유를 한국어 사전 단어로 바꾸면 기술적 뜻은 빠지고 **문자 그대로의 이미지만 남는다.** "고아 노드"를 읽는 한국 독자는 부모 노드가 없다는 뜻보다 고아라는 단어를 먼저 본다.
판별법: **한국 개발자가 동료에게 말할 때 그 단어를 쓰는가.** 쓰지 않으면 ① 뜻을 풀어 쓰거나 ② 영어 원어를 그대로 둔다.

✗ "cron이 몇 달간 **조용히 실패**했습니다." ○ "cron이 몇 달간 실패했는데 **아무 표시가 없었습니다**." / "에러도 경고도 없이 실패했습니다."
✗ "**고아 노드**가 남습니다." ○ "**연결이 끊긴 노드**가 남습니다." / "부모가 없는 노드" / "`orphan` 노드"
✗ "**우아하게 종료**합니다." ○ "하던 요청을 마치고 종료합니다."
✗ "**순진한 구현**입니다." ○ "가장 단순한 구현입니다." / "최적화하지 않은 구현"
✗ "**매달린 포인터**" ○ "해제된 메모리를 가리키는 포인터" / "`dangling` 포인터"
✗ "**신선하지 않은 캐시**" ○ "오래된 캐시" / "갱신되지 않은 캐시"

이미 한국 개발자 사이에서 굳은 말은 그대로 쓴다 — `메모리 누수`, `좀비 프로세스`, `병목`, `핫픽스`, `데드락`. 굳었는지 애매하면 풀어 쓰는 쪽이 안전하다.

**번역한 뒤 소리 내어 읽어 본다.** 입에 안 붙으면 어순·표현을 바꾼다.
한국어는 동사 중심이고 주어를 자주 생략한다 — 영어의 주어·copula·존재구문을 그대로 살리려 하지 않는다.

**한국어 어순으로 — 영어 도치 강조 ✗.**
"흥미로운 건 X입니다 / 절묘한 건 ~입니다 / 중요한 건 ~다" 같은 영어 cleft 강조 도치는 한국어로 풀어 쓴다.
✗ "흥미로운 건 공개 시점이다. ... 시점이 절묘하다."
○ "공식 문서가 나온 시점이 공교롭다. 사람들이 유출 코드를 뜯어보던 무렵, Anthropic이 ..."
강조는 도치 대신 단정 평서문에 부연을 얹는다.
"칼로 자른 듯" 같은 진부한 비유도 피한다.

**기간은 부사어로 동사 앞에 — 완료시제 직역 ✗.**
영어 완료시제(`has been ~ing for N years`)를 "~한 지 N년째입니다"로 옮기면 한국어로 어색하다.
기간을 명사화해 문장 끝 계사로 받지 말고, **부사어로 주어 뒤·동사 앞에** 둔다.
✗ "이 문제를 연구한 지 13년째입니다."
○ "**13년 동안** 이 문제를 연구했습니다."

**인물을 화제로 처음 꺼낼 땐 주제 표지 `는`.**
주격 `가`는 그 인물을 새 정보로 지목하는 느낌이라, 화제를 여는 자리에서는 `는`이 자연스럽다.
✗ "Advait Sarkar**가** 마이크로소프트 리서치에서 일합니다."
○ "Advait Sarkar**는** 마이크로소프트 리서치에서 일합니다."

**한글 용어에 영어 병기 괄호 ✗.**
`렌즈(lens)`, `프로바케이션(provocation)`, `생각의 도구(tool for thought)`처럼 한국어 단어 뒤에 설명용 영어를 괄호로 덧붙이지 않는다.
그냥 `렌즈`, `프로바케이션`으로 쓴다 — 번잡하고, `**조수(assistant)**`처럼 굵게 안에 넣으면 닫는 `**`가 `)` 뒤·한글 앞에 와 마크다운 굵게가 깨진다(2-1절).
영어 원어가 이해에 꼭 필요하면 괄호 대신 문장으로 풀거나, 굵게 밖으로 빼고 굵게는 한글에만 건다.
단, 이 조항은 "한국어 단어에 영어를 병기"하는 패턴만 막는다 — 제품 UI 모드·플래그명(`Plan mode`, `Auto-accept edits`), 명령·식별자(`diff`), 영어권 인명·고유명사는 아래 조항대로 **영어 원표기 그대로** 쓴다.

**과장된 관용구·비유 ✗ — 사실을 그대로 적는다.**
힘줘 말하려고 몸짓 큰 표현을 빌려 오지 않는다. 문장 자체가 단정하면 충분하다.
✗ "문서는 이 점을 **못 박습니다**." ○ "문서는 이 점을 분명히 적어 뒀습니다."
✗ "**쐐기를 박는** 대목입니다." ○ "여기서 결론이 갈립니다."
같이 걷어낼 표현: `쐐기를 박다`, `쐐기를 박듯`, `방점을 찍다`, `선을 긋다`, `못을 박듯`,
`화룡점정`, `신의 한 수`, `게임 체인저`, `판을 뒤집다`, `업계를 뒤흔들다`, `주목할 만하다`.

**전달자의 시선 — 정보 나열이 아니라, 배운 사람이 옆에서 설명하는 글.** (2026-09-16 피드백)
이 블로그의 글은 백과사전 항목이 아니다. **방금 이해한 사람이 다른 사람에게 풀어 주는 글**이고, 독자가 필자의 생각 흐름을 따라오게 친절하게 쓴다. 사실은 같아도 "정의 → 정의 → 정의"로 떨구면 정보 제공식이고, "궁금증 → 답 → 그래서 다음 궁금증"으로 이으면 전달식이다.

- **섹션 사이를 질문으로 잇는다.** 앞 섹션이 남긴 궁금증을 다음 섹션 첫 줄이 받는다. "그럼 그 76억 개 숫자는 파일 안에 어떻게 들어 있을까요?" → 바로 답. 답을 미루는 빌드업이 아니라 **다음 칸으로 넘어가는 연결**이다.
- **왜 그런지를 한 줄 덧붙인다.** "Decode 는 메모리 대역폭이 병목입니다"에서 멈추지 않고 "계산은 작은데 매번 가중치 전부를 읽어야 하기 때문입니다"까지.
- **필자가 헷갈렸던 대목을 밝힌다.** "저도 처음엔 7B 가 파일 크기인 줄 알았습니다" 같은 한 줄이 독자를 같은 눈높이로 데려온다. 글당 한두 번.
- **독자가 어디까지 왔는지 짚어 준다.** "여기까지 오면 1편의 `k_proj` 가 왜 작았는지 보입니다"처럼 앞 내용과 이어 준다.
- 이 장치들은 "두괄식·메타 도입 ✗" 와 충돌하지 않는다. 질문 바로 다음 줄에 답이 있으면 두괄식이고, "살펴봅시다"처럼 답 없이 안내만 하면 메타 도입이다.

**풀어서 말하듯 — 압축 문체 ✗.** (2026-09-16 피드백)
규약을 다 지켜도 남는 번역투의 정체는 **압축**이다. 정보를 수사·명사구·중점(·) 나열로 눌러 담으면 문법은 맞는데 한국 사람이 말하는 문장이 아니게 된다. 옆 사람에게 설명할 때 쓰는 길이로 푼다.

✗ "일반 로드 밸런서와 다른 점은 **셋입니다**."
○ "일반 로드 밸런서와 차이점은 **세 가지가 있습니다**."
✗ "그 안에 든 것은 문장도 지식도 아니고 76억 개의 숫자입니다."
○ "그 어마어마한 용량을 차지하는 건 76억 개의 숫자입니다."
✗ "7B·70B 라는 이름, 학습과 추론의 차이, 벡터 DB 와의 구분은 전부 이 숫자가 무엇인지**에서 출발합니다**."
○ "파라미터 크기를 이해하면 학습과 추론의 차이, 벡터 DB 와의 구분이 **가능해집니다**."

- **수사 단독 술어 ✗.** "~은 셋입니다 / 둘로 나뉩니다 / 넷이면 됩니다" → "세 가지가 있습니다 / 두 가지로 나뉩니다 / 네 가지면 됩니다".
- **명사 술어로 끝내는 안내 문장 ✗ → 동사로 끝낸다.** "다음 편은 GPU 메모리와 양자화**입니다**" → "다음 편에서는 GPU 메모리와 양자화에 대해서 **알아보겠습니다**". "이 글의 주제는 X 입니다" → "이 글에서는 X 를 다룹니다".
- **추상 명사 마무리 ✗.** "~의 정체 / ~하는 지점 / ~가 갈리는 자리 / ~의 거래" 처럼 문장을 명사로 눌러 닫지 않는다. 동사로 끝낸다.
- **중점(·) 나열로 개념 셋을 한 문장에 ✗.** 나열은 본문 불릿이나 표로 내리고, 문장에는 하나만.
- **"X도 Y도 아니고 Z" / "X 가 아니라 Y" 대조 프레이밍 ✗.** 그냥 Z 를 말한다.
- **추상 동사 ✗ → 독자 이득으로.** "~에서 출발합니다 / ~가 갈립니다 / ~를 결정합니다" 대신 "~를 이해하면 ~할 수 있습니다 / ~를 알면 ~가 보입니다".
- **완화 군더더기 ✗.** "~라고 부르는 일이 많습니다 / ~인 경우가 많습니다 / ~하는 경향이 있습니다" → "~라고 부릅니다 / 보통 ~입니다 / 대체로 ~합니다". 단정할 수 있으면 단정한다.
- **"풀린다(의문이)·갈린다·잡힌다" 같은 비유 자동사 ✗.** "여기서 B 가 풀립니다" → 그 문장을 지우고 사실을 바로 적는다. "숫자 하나가 풀립니다" → "숫자 하나를 설명할 수 있습니다".
- **"나란히 놓으면 이렇습니다" ✗ → "비교해 보면 아래와 같습니다".** 표를 소개하는 문장은 동작 그대로.
- **"~가 보입니다"(알게 된다는 뜻) ✗ → "~를 알게 됩니다".** "차이가 보입니다"는 눈에 보이는 것에만. "한 개도" → "하나도".
- **순화어 ✗ → 개발자가 쓰는 말.** "내려받다"→"다운받다", "파일 네 개가 옵니다"→"파일 네 개가 들어있습니다", "올리다"(업로드)는 그대로. 기준은 옆자리 개발자가 실제로 쓰는 단어.

**동작은 표준 동사로 — 손맛 동사·비유 목적어 ✗. 기준은 Papago.**
기술 글에서 가장 자주 남는 어색함은 문법이 아니라 **어휘**다. 문법은 맞는데 한국 개발자가 안 쓰는 말이 된다.
원인은 두 가지다. ① 동작을 사전 1번 뜻의 동사 대신 구어·비유 동사로 적는 것, ② 제품·기능을 주어로 세우고 비유 목적어를 붙이는 것.
판별법은 단순하다 — **Papago 가 이렇게 번역하겠는가.** Papago 는 실행하다·관리하다·확인하다·표시하다처럼 표준 어휘로 곧게 옮긴다. 그 어휘를 쓴다.

✗ "터미널 한 개로는 에이전트 다섯을 **못 굴립니다**."
○ "터미널 하나로는 에이전트 다섯 개를 동시에 **실행할 수 없습니다**."
✗ "Orca는 책상 위에 **관제탑을 올립니다**."
○ "Orca는 여러 에이전트를 한 화면에서 **관리하는 도구입니다**."
✗ "diff 는 검증을 명령 단위로 **당겨 놓습니다**."
○ "diff 를 붙이면 검증이 명령 단위로 **앞당겨집니다**."
✗ "diff 는 파일에 남은 **자국이라는 경로를 끼워 넣습니다**."
○ "diff 는 파일이 실제로 어떻게 바뀌었는지를 **보여 줍니다**."

- **손맛 동사 → 표준 동사.** `굴리다`→실행하다·운영하다, `뜯어보다`→분석하다·살펴보다, `걷어내다`→제거하다·빼다, `당기다/당겨 놓다`→앞당기다, `쌓다`(작업을)→진행하다·누적하다, `쥐다`(정보를)→갖다·받다, `짚다`→지적하다·가리키다, `뱉다`(출력을)→출력하다, `밀어 넣다`→추가하다·넣다, `물다`(값을)→치르다·부담하다.
  `돌리다`(테스트·빌드·서버를)·`띄우다`(서버·컨테이너·에이전트를)는 개발자 일상어라 허용한다.
- **비유 목적어·비유 명사 ✗.** `관제탑`, `자국`, `경로`(추상 의미), `자리`(추상 의미), `물건`(추상 의미), `눈금`, `청구서`(비유), `빨간불`(비유). 실제 대상이 무엇인지 그 이름으로 적는다 — 관제탑이 아니라 "관리 화면", 자국이 아니라 "변경 내역".
- **제품·기능 주어 + 비유 서술 ✗ → 정의문·기능 서술.** "X 는 ~하는 도구입니다 / X 를 쓰면 ~할 수 있습니다"로 곧게 적는다. 제품이 무엇을 "올리고·세우고·열고·닫는" 식의 의인화 서술은 쓰지 않는다.
- **한 글에 비유는 한둘.** 그것도 사실 문장이 아니라 감상 문장에서만. 사실을 전하는 문장은 전부 표준 동사다.

**수량은 단위 명사와 함께 — 수사 단독 ✗.**
✗ "에이전트 다섯을" / "터미널 한 개로" / "파일 수백이"
○ "에이전트 다섯 대를(개를)" / "터미널 하나로" / "파일 수백 개가"
에이전트·프로세스·세션은 `개`가 기본이고, 여러 인스턴스를 서버처럼 세는 맥락이면 `대`도 된다. 한 글 안에서는 하나로 통일한다.

**연결어미 뒤 쉼표 ✗.**
`~고,` `~며,` `~지만,` `~면서,` `~아서/어서,` 뒤에 쉼표를 찍지 않는다. 영어 콤마를 그대로 옮긴 습관이라 한 글에 대여섯 번만 쌓여도 번역투로 읽힌다. 쉼표를 빼고, 문장이 길어지면 끊는다.
✗ "모델이 명령을 부르고, 하네스가 결과를 돌려주고, 모델이 그걸 읽습니다."
○ "모델이 명령을 부르면 하네스가 결과를 돌려주고 모델이 그걸 읽습니다."
○ "모델이 명령을 부릅니다. 하네스가 결과를 돌려주면 모델이 그걸 읽습니다."
예외: 쉼표가 없으면 수식 관계가 헷갈리는 긴 문장 — 그때는 쉼표 대신 문장을 끊는 쪽을 먼저 본다.

**"X가 아니라 Y" 틀은 한 글에 두 번까지.**
"실패가 아니라 성공의 모습", "거짓말이 아니라 관측 부족", "처리량 눈금이 아니라 검토 부채의 눈금" — 한두 번은 강조지만, 매 문단이 이 틀이면 글 전체가 "반전 선언"처럼 읽힌다. 영어 AI 글의 `not X, but Y` 가 그대로 넘어온 것이다. 셋째부터는 Y 만 평서로 적는다.
✗ "diff 는 그 판정을 대신하는 물건이 아니라, 판정이 돌아오기 전까지의 구간을 덜 캄캄하게 만드는 물건입니다."
○ "diff 는 그 판정을 대신하지 않습니다. 판정이 나오기 전까지의 구간을 확인할 수 있게 해 줄 뿐입니다."

**번역이 아니라 "읽고 이해한 사람의 설명".**
원문을 한 번 소화한 사람이 한국 독자에게 직접 풀어 주는 톤으로 쓴다.
"문서를 옮긴" 느낌을 내는 다음을 걷어낸다:


- **매 문장 출처 표딱지 ✗.**
  "Anthropic에 따르면"을 문장마다 달지 않는다.
  글 전체가 그 원문 정리임은 맨 위 인용구·말미 출처로 이미 약속돼 있다.
- **단문 나열 ✗ — 맥락으로 잇는다.**
  사실 A·B·C를 토막으로 떨구지 말고, 원문에 이미 있는 인과·배경을 살려 "왜 그런지"를 독자가 따라오게 흐름을 만든다.
- **모를 용어는 한 줄 풀어 준다.**
  약어·전문어는 원문 사실 범위 안에서 가볍게 짚어 준다(가르치듯).
  단, 추측·배경지식 창작 ✗.

여전히 지켜야 할 선: 사실·수치·인용은 정확하게, 지어내지 않는다.
감상은 얼마든지 얹되 사실로 둔갑시키지 않는다 — 무엇이 원문이고 무엇이 내 생각인지 드러나게.

**기술 용어·명령·모드명은 음차하지 않는다 — 영어 그대로.**
`diff` 같은 명령·코드·식별자처럼 개발자가 영어 그대로 쓰는 말은 한글 소리로 옮기지 않는다.
"디프"가 아니라 `diff`로 쓴다.
음차하면 어색하고 검색도 되지 않는다.
영어 그대로 쓰되, 명령·식별자는 `인라인 코드`로 감싸도 좋다.

**제품 UI의 모드·메뉴·플래그 이름도 영어 원표기.**
한국 개발자도 실제로 영어로 부르는 이름은 영어 그대로 적는다.
✗ "기본 모드 / 자동 수락 편집 모드 / 플랜 모드" — 어색한 번역.
○ "**Default** / **Auto-accept edits** / **Plan mode** / **Auto mode**" — 원표기 그대로.
설명만 한국어로 곁들인다.

단, 이미 한국어로 굳어진 외래어(코드·테스트·에이전트·플러그인·리팩터링 등)는 그대로 둔다.

**인명·회사명·제품명도 영어 원표기 — 한글 음차하지 않는다.**
원문에 영어로 나온 사람 이름·회사 이름·제품 이름은 알파벳 그대로 적는다.
✗ "다리오 아모데이 / 다니엘라 아모데이 / 오픈에이아이 / 클로드 코드"
○ "**Dario Amodei** / **Daniela Amodei** / **OpenAI** / **Claude Code**"
음차하면 검색도 안 되고, 같은 사람이 글마다 다른 표기로 갈린다.
처음 나올 때 한글 설명을 곁들이는 건 괜찮다 — "Anthropic CEO **Dario Amodei**".
단, 한국 인물·기업이거나 이미 한국어 표기가 공식인 경우(카카오·네이버·토스 등)는 한국어로 적는다.

**호칭·관계어는 사실을 확인하고 쓴다 — 성별을 넘겨짚지 않는다.**
"형·오빠·누나·언니·아들·딸" 같은 말은 관계와 성별이 확인될 때만 쓴다.
✗ Daniela Amodei가 Dario Amodei를 가리켜 "**형**" — 남매이므로 "**오빠**"다.
확인이 안 되면 관계어를 성별 없이 적는다 — "남매", "형제자매", "손위 형제", "동생".
직함·역할(대표·창업자·연구원)도 마찬가지로 원문에서 확인된 것만 쓴다.

**두괄식 — 두괄은 대제목이 담당.**
대제목이 그 섹션의 결론을 이미 담는다.
본문은 `핵심은 이겁니다 / 규모가 큽니다` 같은 군더더기 도입부 없이 바로 내용으로 들어간다.
글 전체도 맨 위 인용구에서 결론(무엇을·왜)부터 제시한다.
"숨어 있었다 → 알고 보니 ~였다" 식 미괄식 빌드업 ✗.

**서두 — 워밍업으로 빌드업하고, 제목이 만든 궁금증을 첫 문단 안에서 푼다.** (2026-09-16 피드백)
제목이 궁금증을 만들었으면 독자는 답을 기대하고 들어온다. 답은 첫 문단 **안에서** 준다 — 다만 첫 줄부터 답을 던지는 게 아니라 **독자가 겪은 장면으로 워밍업한 뒤** 준다.

1. **워밍업 — 독자가 겪은 장면.** "Hugging Face 에서 모델을 고를 때 이름 뒤에 `7B`·`70B` 같은 숫자를 많이 보셨을 겁니다."
2. **질문.** "이 B 는 뭘 뜻할까요?"
3. **답.** "파라미터 개수입니다. B 는 Billion, 7B 면 70억 개입니다."
4. **예시로 실감.** "예를 들어볼까요? Qwen2.5-7B 를 다운받으면 15.2GB 짜리 파일 네 개가 들어있습니다. 그 어마어마한 용량을 차지하는 게 76억 개의 숫자입니다."
5. **이걸 알면 뭐가 보이는지.** "파라미터 크기를 이해하면 학습과 추론의 차이, 벡터 DB 와의 구분이 가능해집니다."

이 1~5 는 첫 `##` 앞의 **도입 문단**(또는 첫 섹션)에 들어간다. 맨 위 `>` 요약은 별개다 — 카드·OG·검색에 나가는 **1~2문장 발췌**이지 답변 칸도, 도입 문단의 복사본도 아니다. `>` 를 "아닙니다." 처럼 직답으로 시작하지 않고, **도입 문단과 같은 문장을 반복하지 않는다**(같은 화면에 두 번 보인다). `>` 는 "이 글이 무엇을 어떻게 정리했는지" 한두 문장, 도입 문단은 워밍업 서사 — 역할을 나눈다.

**시리즈 글이어도 본문에서 "N편에서 본 / N편에서 다룰" 을 쓰지 않는다.** 그 글만 검색으로 들어온 독자가 대부분이다. 앞 글의 사실이 필요하면 한 줄로 다시 적고("Qwen2.5-7B 파일 안의 `q_proj` 표는 3584 × 3584 입니다"), 다음 글 예고는 "다음 글에서 ~에 대해서 알아보겠습니다" 로. 순서 안내는 시리즈 상자가 한다.

워밍업은 "살펴봅시다"류 메타 문장이 아니라 **독자의 경험**이다. 반대로 워밍업 없이 답부터 던지면 백과사전이 되고, 답을 계속 미루면 낚시가 된다. 둘 다 ✗.

**요약·문장은 사실부터 직진 — 부사어구·메타 도입 ✗.**
요약 인용구와 각 문단은 **첫 문장이 곧 정보**가 되게 쓴다.
"그런데 ~ 다음 수가 좀 의외였죠", "참 절묘하죠", "따라가 봅니다", "살펴봅시다" 같은 감상·전환·메타 도입은 빼고 사실을 바로 던진다.
✗ "유출 사고부터 동작 원리까지 따라가 봅니다." — 글을 안내하는 메타 문장.
✗ "이 글은 그 질문 하나를 끝까지 따라갑니다." / "그 과정을 순서대로 따라가 보겠습니다." — 같은 부류. 영어 follow 의 직역이라 더 어색하다. 그냥 다음 문단으로 넘어간다.
✗ "그런데 Anthropic의 다음 수가 의외였죠 —" — 감상으로 사실 전달을 늦춤.
○ "Anthropic은 사태 직후 공식 문서에 동작 원리를 정리해 공개했습니다." — 사실 직진.
감상·해석은 본문 안에서 사실 다음에 얹는다.

**날짜는 알 수 있는 만큼 정확히 — 사건 글은 일자까지.**
유출·발표·출시 같은 사건은 "2026년 3월"이 아니라 **"2026년 3월 31일"**처럼 일자까지 적는다.
원문에 일자가 있으면 반드시 옮기고, 월 단위로 뭉뚱그리지 않는다.
일자가 정말 없을 때만 월·분기로 좁힌다(추측한 날짜 ✗).

**대제목 = 그 글의 주제어가 들어간 라벨.**
짧은 명사구가 기본, 짧은 서술형도 가능 — 어느 쪽이든 **그 글에서만 나오는 고유 주제어**(고유명사·제품명·핵심 키워드)가 들어가야 한다.
대제목만 위→아래로 훑어도 원문의 전모가 잡히게 한다.
그 아래 단락은 "더 알고 싶은 사람만 펴 읽는" 상세가 되게 한다.

```markdown
> (인용구 = 카드·검색·페이지 두괄식 훅이 같이 쓰는 단일 요약 — 제목의 질문에 대한 직답 + 예시 + 이 글에서 얻는 것. -습니다 체)

(강연·발표 영상 글이면 여기에 발표자 소개 문단 — 아래 "발표자 소개" 절 참고)

## (그 섹션의 주제어 라벨 — 짧은 명사구가 기본)
도입부 없이 바로 사실부터. 한 섹션은 한두 단락으로 가볍게("분량과 호흡" 절).

## (다음 주제 라벨 — 발표의 전모/이름)
무엇을·왜. 산문이 기본이되, 스캔되는 사실 묶음(수치·항목 나열)은 불릿으로 떼어 내도 좋다.

## (다음 주제 라벨 — 규모·결과 등 한 토막씩)
한 헤드라인 = 한 메시지. 빠뜨리면 안 되는 사실 위주로 짧게.

## (마지막 라벨 = 핵심·시사점, 또는 글쓴이의 마무리)
원문이 짚는 메시지를 정리하거나, 글을 덮으며 든 생각으로 맺는다. 발언은 발화자 명시.

## 참고 자료
- 원문: [제목 — Anthropic](원문 URL)

> 이 글은 Anthropic 원문을 번역·정리한 것입니다. 원저작권은 Anthropic에 있습니다.
```

> 위 골격은 **한 가지 예시**다. 모든 글이 "요약 → 헤드라인 4개 → 마무리 라벨 → 참고 자료"를 그대로 따를 필요는 없다.
> 글의 길이, 헤드라인 개수, 마무리 절의 유무·위치는 원문 결에 맞게 매번 달라져야 한다.
> 같은 형태가 직전 글과 그대로 겹치면 그게 자동화 신호다 (2-3절).

원칙: **사실은 정확하게 ✓ / 사실 왜곡·창작 ✗.**
요약·재구성·개인 감상은 자유.
출처(원문 링크)는 필수 — `## 참고 자료`에 적는다.
저작권 줄(맨 아래 `>` 인용)은 **기본은 생략**한다.
참고 자료에 출처 링크가 있으면 그것으로 충분하다.
원문이 라이선스를 강하게 요구하거나, 번역·재구성 비중이 커서 출처 강조가 필요한 글에만 예외적으로 단다.

맨 위 `>` 인용구는 `.prose blockquote` 스타일(`src/app/globals.css`)이 잡혀 있어 핵심 요약 박스로 잘 보인다.
이 인용구가 페이지에서 보는 두괄식 훅이자 **카드·검색·OG·RSS의 excerpt 컬럼에도 그대로 들어가는 같은 문장**이다.
결론(무엇을·왜)을 여기서 단정해 적는다.

### 분량과 호흡 — 헤드라인은 많아도, 한 칸은 가볍게

길이의 문제는 헤드라인 개수가 아니다.
**한 헤드라인 아래에 글이 빽빽하게 들어차는 것**이 문제다.
화면이 글자로 꽉 차면 독자는 읽기도 전에 지친다.
헤드라인은 필요한 만큼 쓰되, 그 아래는 가볍게 비운다.

- **한 헤드라인 = 한 토막.**
  그 섹션은 한 가지만 말한다. 한 단락, 길어도 두 단락.
  단락은 2~4문장. 할 말이 더 있으면 헤드라인을 새로 판다.
- **긴 문단은 무조건 쪼갠다.**
  대여섯 문장이 한 덩어리로 이어지면 벽처럼 보인다.
  짧은 문단·한 줄 문장을 섞어 숨 쉴 틈을 만든다.
- **스캔되는 사실은 불릿(`-`)으로 떼어 낸다.**
  수치 묶음·복수 항목·"이런 것들"이 나열되면 한 문단에 욱여넣지 말고 짧은 불릿으로 시각적 여백을 만든다.
  단, 산문 흐름을 끊는 한두 문장은 그대로 두는 게 낫다 — 모든 단락을 불릿으로 바꾸면 글이 발표 자료처럼 차가워진다.
- **리스트는 동격인 항목 3개 이상일 때만.**
  단일 동작·인과 흐름·"A이고 그래서 B" 같은 자연스러운 산문은 리스트로 바꾸지 않는다.
  ✗ "체크포인트는 ~다.\n- 스냅샷을 떠 둠\n- Esc 두 번으로 복구\n- 단, 외부 영향은 못 막음" — 한 장치의 동작 설명이라 산문이 자연스러움.
  ○ 모드·갈래·항목처럼 **병렬 비교**가 필요한 곳에서만 리스트.
- **다 담지 않는다.**
  원문의 곁가지·반복 설명·부연 예시는 덜어낸다.
  핵심 사실과 결론만 남기고, 나머지는 한 문장으로 압축하거나 버린다.
- **시각자료·헤드라인으로 끊어 준다.**
  설명이 길어지는 곳은 카드로 정리하거나 헤드라인으로 자른다.
  본문과 시각자료가 같은 말을 반복하지는 않는다 — 한쪽이 요약하면 다른 쪽은 짧게.

### 발표자 소개 — 강연·발표 영상 글에 한해

강연·인터뷰·발표 영상을 다루는 글은, 맨 위 `>` 요약 **바로 다음·첫 `##` 헤드라인 앞**에 **발표자 소개 문단**을 둔다.
본문에 들어가기 전에 "이 사람이 누구인가"를 먼저 짚어, 독자에게 호기심을 주고 글에 신빙성을 싣는다.

- 담을 것: 발표자가 **누구인지**(이름·소속), **신뢰를 뒷받침하는 이력·업적**, **지금 하는 일**.
  2~4문장으로 자연스럽게 풀어 준다.
- 독자가 "이 사람 이야기라면 들어볼 만하다"고 느끼게 하되 — **사실만**.
  과장·미사여구·추어올리는 수식 ✗.
- 영상이 발표자를 충분히 소개하지 않으면 WebFetch로 공개된 사실을 확인한다.
  확실하지 않으면 영상이 밝힌 범위로 좁힌다(추측·창작 ✗).
- **첫 문장이 매번 "X는 ~인 사람입니다" 정형으로 시작하지 않게 한다.**
  그 사람의 두드러진 사실(최근 한 일, 화제가 된 이유, 특이한 약력)부터 던지고 정체는 두 번째 문장에서 풀어도 좋다.
  도입 첫 문장 패턴은 매 글마다 의도적으로 다르게 잡는다 (2-3절).
- **발표자가 없는 글**(아티클 번역 등)에는 넣지 않는다.

### GitHub 저장소 소개 글 — 저장소를 다루는 글에 한해

URL-초안 출처가 GitHub 저장소(`github.com/{owner}/{repo}`)면, 그 저장소를 한국 독자에게 소개·정리하는 글이다.
Anthropic 글 번역이 아니라 **저장소의 README와 공개 정보를 풀어 주는 글**이지만, 말투·강조·시각자료 규약은 그대로 적용한다.
요즘 Claude 스킬·AI 도구가 저장소로 많이 공개된다.
"이게 무엇이고 어떻게 쓰는가"를 또렷이 전달하는 것이 목적이다.

근거는 **README와 저장소 페이지에 적힌 내용**이다.
거기 없는 기능·로드맵·수치는 지어내지 않는다(원문 보존 원칙과 같다).

**글 골격**

```markdown
> (요약 = 이 저장소가 무엇이고 왜 흥미로운지 — 무엇을·왜. -습니다 체)

(저장소 소개 문단 — 아래 설명 참고)

## (무엇을 하는 저장소인가 — 서술형 헤드라인)
어떤 문제를 풀고 무엇을 제공하는지. Claude 스킬·AI 도구면 무엇을 자동화·확장
하는지가 중심.

## (설치하고 쓰는 법 — 서술형 헤드라인)
README 기준 설치·실행·구동 방법. 명령은 코드 블록으로.

## Reference
- GitHub Repo: [{owner}/{repo}](저장소 URL)

> 이 글은 {owner}/{repo} 저장소의 README와 공개 정보를 정리해 소개한 글입니다.
```

- **저장소 소개 문단**은 맨 위 `>` 요약 **바로 다음·첫 `##` 앞**에 둔다 — "발표자 소개"와 같은 자리·역할이다.
  누가/어디서 만들었는지(작성자·조직), 얼마나 주목받는지(스타 수), 주 언어·라이선스, 무엇을 위한 저장소인지를 **2~4문장 산문**으로 푼다.
  스타·작성자 같은 메타정보는 별도 표·카드로 빼지 말고 **문장에 자연스럽게 녹인다**.
- **첫 문장이 매번 "{owner}/{repo}는 ~한 저장소입니다" 정형으로 시작하지 않게 한다.**
  저장소의 두드러진 사실(최근 화제, 별난 동작 방식, 만든 사람의 한 줄)부터 던지고 정체는 두 번째 문장에서 풀어도 좋다.
  도입 첫 문장 패턴은 매 글마다 의도적으로 다르게 잡는다 (2-3절).
- `##` 라벨은 가이드일 뿐 — 헤드라인은 "2. 말투" 절대로 그 저장소에 맞는 서술형 문장으로 쓴다.
  동작 원리가 흥미로우면 `## 어떻게 동작하는가` 절을 더해도 된다.
- 설치·실행 명령은 ` ```bash ` 같은 코드 블록에 적는다.
  README에 없는 명령은 만들지 않는다.
  단계가 길면 `step-card`로 흐름을 요약해도 된다(시각자료 한도 유지).
- `## Reference` 절의 헤드라인과 링크 라벨은 영어로 — 섹션 제목은 `Reference`, 저장소 링크는 `GitHub Repo:`, 그 밖의 관련 링크는 `Related:`로 적는다.
- Reference의 저장소 링크는 GitHub 저장소 URL이고, 저작권 표기는 Anthropic이 아니라 저장소·작성자 기준으로 적는다(라이선스를 알면 함께 적어도 좋다).

---

## 2-1. 강조 표기 · 이모지 (밋밋함 방지)

본문이 헤드라인만 있고 문단은 평문뿐이면 읽기 지친다.
**핵심 단어·수치·발언에 강조를 입혀** 눈이 머물 지점을 만든다.
단, 강조는 **희소할수록 강하다** — 한 문단에 강조가 서넛이면 강조가 아니라 잡음이다.
정말 중요한 곳에만.

| 표기 | 쓸 곳 |
|---|---|
| `**굵게**` | 그 단락의 핵심 주장·결론, 빠뜨리면 안 되는 수치/금액/비율 (구절 단위, 문장 통째 ✗) |
| `*이탤릭*` | 약한 강조, 용어를 처음 꺼낼 때, 미묘한 뉘앙스 |
| `` `인라인 코드` `` | 모델·벤치마크·API·명령·기술 식별자 (예: `Mythos Preview`, `CyberGym`, `SWE-bench`). 일반 회사·제품명은 코드로 감싸지 않는다 |
| `> 인용` | 원문 발언(발화자 명시) 또는 원문이 단정해 말한 한 덩어리 |
| `<u>밑줄</u>` | 시각 포인트가 꼭 필요한 곳 한정. 드물게 |
| `<mark>형광펜</mark>` | 글 전체에서 가장 중요한 한 구절. **글당 0~1회 · 매번 같은 위치 ✗** (기본은 빈 채로 둔다) |
| `~~취소선~~` | "이제는 아니다" 식 정정·대비 |

- 한 단락에 굵게는 보통 0~1개.
  같은 단어를 매번 굵게 ✗ (첫 등장만).
- **`<mark>` 형광펜은 빈 채로 두는 게 기본.**
  글 전체에서 정말 남길 한 구절이 있을 때만 1회. 매 글마다 하나씩 넣지 않는다.
- **강조 위치 자체가 정형이 되지 않게 한다.**
  마지막 헤드라인 직전(글 80~90% 지점)에 형광펜이 매번 박히는 패턴은 자동화 티의 대표 사례다.
  넣을 때도 위치(앞·중·끝)는 직전 글과 다르게.
- 인용구·헤드라인 안에는 강조 표기를 넣지 않는다(그 자체가 이미 강조).
- **`**굵게**`는 한글 글자 바로 뒤에서 닫는다.**
  닫는 `**`가 `)`·`%` 같은 문장부호 뒤·한글 앞에 오면 마크다운이 굵게로 읽지 못해 `**`가 화면에 그대로 노출된다.
  `**핵심 개념(Key Concept)**입니다` ✗ → `**핵심 개념**(Key Concept)입니다` ○ — 영문 괄호는 굵게 밖으로 뺀다.
- **이모지(Tossface): 친근하게, 적극적으로 — 단 남발 ✗.**
  이 블로그는 Tossface 컬러 이모지 폰트를 도입했다(폰트 스택 맨 뒤 `"Tossface"`, `src/app/globals.css`).
  딱딱한 평문에 토스페이스 이모지를 곁들여 친근한 톤을 낸다 — 텍스트로 이모지를 쓰면 자동으로 토스 스타일로 렌더된다.
  본문 문장 끝·절 단위에 가볍게, 그리고 ` ```visual ` 카드의 아이콘 자리(`"icon": "💡"`처럼 이모지를 직접)에 쓴다.
  단 장식·도배는 피한다 — 한 문단에 여럿 깔리면 잡음이다. 맨 위 인용구·헤드라인·참고/저작권 줄에는 넣지 않는다.

---

## 2-2. 시각자료 카탈로그 (이해를 돕는 시각자료)

> **먼저 — 시각자료는 글마다 하나씩 있어야 하는 게 아니다.**
>
> 만드는 조건은 하나다. **구조·흐름·설계가 평문만으로는 머리에 안 그려질 때.**
> 여러 갈래가 어떻게 맞물리는지, 무엇이 어떤 순서로 흐르는지, 어느 쪽이 어떻게 갈라지는지 —
> 그림 없이 설명하면 독자가 문장을 두 번 읽게 되는 자리. 거기서만 그린다.
>
> 반대로 이런 이유로는 만들지 않는다:
> - "글에 그림이 하나는 있어야 하니까" — **할당량은 없다. 0개인 글이 절반은 나와도 정상이다.**
> - "수치가 세 개 나왔으니 `stat-card` 하나" — 문장으로 읽히면 문장이 낫다.
> - "소제목마다 하나씩" — 같은 자리에 같은 패턴이 반복되면 그게 가장 명백한 자동화 티다(2-3절).
> - "마무리 감상 칸이 허전해서" — 채우기 위한 채우기 금지.
>
> **그리기 전에 자문한다: "이 그림을 빼면 독자가 이 대목을 이해 못 하나?"**
> 아니라면 빼고 본문 한 문장으로 적는다.
> 그리고 **원문·공식 문서에 이미 그 구조를 그린 그림이 있다면 그걸 쓰는 게 먼저다**(아래 "작성 규칙").

평문·헤드라인만으로 흐름·구조·비율이 머리에 안 그려지는 부분에 한해 시각자료를 얹는다.
본문에 ` ```visual ` 코드 블록 안에 **JSON**(패턴 + 데이터)만 적으면, 블로그가 그 블록을 **React 카탈로그 컴포넌트로 본문에 그대로 렌더**한다.
색은 디자인 토큰이라 라이트/다크 자동 대응.
이미지로 굽지 않으므로 선명하고 본문 폭에 맞게 반응형으로 표시된다.
블록 안에 HTML/CSS는 쓰지 않는다(디자인 일관성은 카탈로그가 책임진다).

> 카탈로그는 **다이어그램·구조·지표**를 일관되게 그리기 위한 것이다.
> 원문에 이미 있는 그림·사진은 카탈로그 대상이 아니다 — 아래 "작성 규칙" 참고.

**작성 흐름**

````markdown
```visual
{
  "pattern": "step-card",
  "alt": "Claude Code의 에이전트 루프 5단계",
  "title": "Claude Code는 에이전트 루프로 작동합니다",
  "steps": [
    { "label": "사용자 요청",   "sublabel": "your prompt",   "icon": "message-circle" },
    { "label": "컨텍스트 수집", "sublabel": "read · search", "icon": "search" },
    { "label": "실행",         "sublabel": "edit · run",    "icon": "edit" },
    { "label": "결과 검증",     "sublabel": "test · verify", "icon": "check" },
    { "label": "작업 완료",     "sublabel": "task complete","icon": "check-check" }
  ]
}
```
````

블로그가 위 블록을 발견하면 해당 카탈로그 컴포넌트를 본문에 렌더한다.
`alt`는 반드시 적는다(접근성·검색 노출에 쓰인다).
디자인 변경은 컴포넌트 쪽에서 한 번 고치면 모든 글의 시각자료에 일관 적용된다.

### 언제 쓰는가

- **직선형 과정·순서·파이프라인** → `step-card` (요청 → 수집 → 실행 → … 한 방향으로 흐르는 단계). 되돌아가 **반복되는 순환**은 step-card로 못 그리니 illustration으로 — 본문이 "반복"을 말하면 시각자료도 반복을 보여야 한다
- **숫자·지표 강조** → `stat-card` (벤치마크 점수, 금액, 점유율 등 1~4개 지표)
- **핵심 인사이트 박스** → `callout-card` (한 문장으로 단정하는 결론·경고·요점)
- **둘·셋 견주기** → `compare-card` (기존 방식 vs 새 방식, A안 vs B안 — 항목을 칼럼으로 나란히)
- **시간 순 흐름** → `timeline-card` (출시 연혁, 사건 순서 — 날짜가 붙는 흐름. 번호 절차는 step-card)
- **원문·강연 인용** → `quote-card` (발표자·원작자가 한 말을 그대로 옮길 때)
- **권장·금지** → `checklist-card` (이렇게 하라 / 이건 피하라 — 체크·금지 표시로 구분)
- **순서 없는 구성요소** → `grid-card` (한 개념을 이루는 갈래·요소를 아이콘 타일로. 순서가 있으면 step-card)
- **짧은 키·설명 나열** → `list-card` (모드 목록, 처리 순서, 키-설명 요약. 단순 `-` 마크다운 리스트보다 한 단계 강조하고 싶을 때)
- **한 줄짜리 짧은 나열** → `bullet-card` (설명 없이 라벨만 — "이런 모습입니다" 류의 압축 목록. 키·설명이 필요하면 list-card)
- **항목 여럿 × 대상 여럿 대조** → **마크다운 표** (대상 2~3개를 항목 4줄 이상으로 견줄 때. 카탈로그 카드가 아니라 본문 표다 — 아래 "마크다운 표" 절)
- **개념·은유 그림** → ` ```illustration ` (구조 스케치, 비유적 그림 — 정확한 수치가 아니라 직관을 그릴 때)

본문에 이미 한 문장으로 충분히 전달된 사실은 시각자료로 또 옮기지 않는다.
**본문이 설명한 구조·결론을 한눈에 보이게 압축**할 때만 쓴다.
시각자료에서 새 사실·추측을 만들어내지 않는다(원문 보존 원칙).

### 공통 옵션

모든 패턴이 공통으로 받는 필드:

| 키 | 타입 | 설명 |
|---|---|---|
| `pattern` | `"step-card" \| "stat-card" \| "callout-card" \| "compare-card" \| "timeline-card" \| "quote-card" \| "checklist-card" \| "grid-card" \| "list-card" \| "bullet-card"` | 카탈로그 패턴 ID. 필수 |
| `alt` | string | 이미지 alt 텍스트. 필수 (접근성·SEO) |
| `eyebrow` | string? | 카드 상단 작은 카테고리/맥락 라벨 (예: `HOW CLAUDE CODE WORKS`). **선택** |
| `title` | string? | 카드 메인 타이틀. **명사형·문어체**로 — `~방식`·`~구조`·`~흐름` 같은 명사 종결, `~합니다`체 ✗. 한 줄·30자 이내. **선택** |
| `accent` | `"primary" \| "accent" \| "info" \| "success" \| "warn" \| "danger" \| "mute"` | 카드 강조 색. 기본 `primary` |

> **`eyebrow`·`title`은 선택이다.**
> 카드 하나로 메시지가 분명하면 둘 다 생략하는 게 깔끔하다 — 본문 문단이 이미 맥락을 잡아 주므로 제목 없는 카드가 오히려 군더더기 없이 낫다.
> 제목을 쓸 땐 반드시 명사형(예: "카탈로그 구성 방식" ○ / "카탈로그는 이렇게 구성됩니다" ✗).
> 라이트·다크는 워커가 두 벌 자동 생성하므로 따로 적지 않는다.

### `accent` — 미리 정의된 7색만 쓴다 (하드코딩 ✗)

색상은 디자인 토큰(`src/app/globals.css`)에 묶여 있어 라이트/다크 자동 대응된다.
**역할마다 색을 달리** 입힌다 — 같은 카드를 모두 같은 색으로 깔지 않는다.

| 클래스 | 색 | 쓸 곳 |
|---|---|---|
| `primary` | 블루 | 기본 처리 단계·메인 흐름 |
| `accent`  | 퍼플 | 보조 분기·대안 경로 |
| `info`    | 티얼 | 외부 입력·소스(사용자, API, 원본 데이터) |
| `success` | 그린 | 최종 결과·출력·성공 |
| `warn`    | 옐로우 | 주의·조건부·인간 검토 |
| `danger`  | 레드 | 실패·차단·에러 경로 |
| `mute`    | 그레이 | 부가 정보·비활성·각주성 노드 |

### 패턴별 명세

**`step-card` — 과정/순서**

| 키 | 타입 | 설명 |
|---|---|---|
| `steps` | `Step[]` | 2~8개. 데스크톱에서 **한 줄로 나란히** 펼쳐진다(카드가 본문보다 넓어짐). 3~6개 권장 |

`Step` 형식:

| 키 | 타입 | 설명 |
|---|---|---|
| `label` | string | 단계 이름 (한 줄, 8자 이내 권장) |
| `sublabel` | string? | 영문 보조 라벨(예: `your prompt`). 한 줄, 16자 이내 |
| `icon` | string | Lucide 아이콘 이름. 아래 화이트리스트 참조 |
| `accent` | string? | 그 단계만 다른 색을 쓰고 싶을 때 (드물게) |

`step-card`는 단계 번호(`01`, `02`…)를 카탈로그가 자동으로 매긴다.
모든 단계 카드는 동일한 스타일로 렌더된다(특정 단계 강조 없음).

**`stat-card` — 숫자 강조**

| 키 | 타입 | 설명 |
|---|---|---|
| `stats` | `Stat[]` | 1~4개. 데스크톱에서 **한 줄로 나란히** 펼쳐진다(카드가 본문보다 넓어짐) |

`Stat` 형식:

| 키 | 타입 | 설명 |
|---|---|---|
| `value` | string | 핵심 숫자/금액/비율 (예: `83.1%`, `1억 달러`, `27년`) |
| `label` | string | 무엇을 가리키는 숫자인지 (한 줄, 16자 이내) |
| `caption` | string? | 출처·기간·맥락 (한 줄, 24자 이내) |
| `icon` | string? | Lucide 아이콘 이름. 있으면 지표 위에 작은 아이콘이 붙어 더 풍부해진다 (선택) |
| `accent` | string? | 그 지표만 다른 색을 쓰고 싶을 때 |
| `delta` | `{ value: string, direction: "up" \| "down" \| "flat" }`? | 전기 대비 증감 표기 (있을 때만) |

**`callout-card` — 핵심 인사이트 박스**

| 키 | 타입 | 설명 |
|---|---|---|
| `icon` | string | Lucide 아이콘 이름 |
| `heading` | string | 한 문장 결론 (40자 이내) |
| `body` | string? | 한두 문장 부연 (120자 이내) |

**`compare-card` — 둘·셋 견주기**

| 키 | 타입 | 설명 |
|---|---|---|
| `columns` | `Column[]` | 2~3개. 나란히 배치되어 비교된다 |

`Column` 형식:

| 키 | 타입 | 설명 |
|---|---|---|
| `label` | string | 칼럼 이름 (예: `에이전트 방식`). 한 줄, 14자 이내 권장 |
| `caption` | string? | 칼럼 한 줄 부연 (20자 이내) |
| `icon` | string? | Lucide 아이콘 이름 (선택) |
| `accent` | string? | 칼럼별 색. 보통 한쪽은 `mute`/`danger`, 다른 쪽은 `primary`/`success` |
| `points` | string[] | 1~6개. 각 항목 한 줄 (35자 이내 권장) |

**`timeline-card` — 시간 순 흐름**

| 키 | 타입 | 설명 |
|---|---|---|
| `events` | `Event[]` | 2~6개. 세로 흐름으로 배치 |

`Event` 형식:

| 키 | 타입 | 설명 |
|---|---|---|
| `date` | string | 날짜/시점 (예: `2024.06`). 한 줄, 10자 이내 권장 |
| `label` | string | 사건 이름 (한 줄, 20자 이내 권장) |
| `description` | string? | 한 줄 부연 (48자 이내) |
| `icon` | string? | Lucide 아이콘 이름. 없으면 점으로 표시된다 |
| `accent` | string? | 그 사건만 다른 색을 쓰고 싶을 때 |

> 번호가 의미 있는 **절차**는 `step-card`, 날짜·연혁 같은 **시점**이 핵심이면 `timeline-card`.

**`quote-card` — 원문·강연 인용**

| 키 | 타입 | 설명 |
|---|---|---|
| `quote` | string | 인용문 한 단락 (110자 이내 권장). **원문 그대로** — 지어내지 않는다. 따옴표는 카탈로그가 자동으로 감싸므로 직접 넣지 않는다 |
| `attribution` | string? | 말한 사람 이름 (20자 이내) |
| `role` | string? | 직함·소속 (24자 이내) |

`quote-card`는 `eyebrow`·`title`을 렌더하지 않는다 — 인용문과 발언자만 보여 준다.
차분한 중립 배경이라 `accent`는 발언자 줄의 작은 강조선에만 쓰인다.

**`checklist-card` — 권장·금지**

| 키 | 타입 | 설명 |
|---|---|---|
| `items` | `Item[]` | 2~8개 |

`Item` 형식:

| 키 | 타입 | 설명 |
|---|---|---|
| `text` | string | 항목 한 줄 (40자 이내 권장) |
| `state` | `"do" \| "dont" \| "neutral"` | 권장(체크·그린)·금지(엑스·레드)·중립(대시·그레이). 기본 `do` |
| `caption` | string? | 항목 한 줄 부연 (40자 이내) |

`checklist-card`의 항목 색은 `state`로 정해진다 — `accent`는 항목 표시에 영향을 주지 않는다.

**`grid-card` — 순서 없는 구성요소**

| 키 | 타입 | 설명 |
|---|---|---|
| `items` | `Item[]` | 2~6개. 데스크톱에서 **한 줄로 나란히** 펼쳐진다(카드가 본문보다 넓어짐). 3~5개 권장 |

`Item` 형식:

| 키 | 타입 | 설명 |
|---|---|---|
| `title` | string | 요소 이름 (한 줄, 14자 이내 권장) |
| `description` | string? | 한두 줄 설명 (48자 이내) |
| `icon` | string | Lucide 아이콘 이름 |
| `accent` | string? | 그 타일만 다른 색을 쓰고 싶을 때 |

> 갈래에 **순서·번호**가 있으면 `step-card`, 그냥 나열되는 **구성요소**면 `grid-card`.

**`list-card` — 키·설명 나열 (모드, 처리 순서, 요약)**

| 키 | 타입 | 설명 |
|---|---|---|
| `items` | `Item[]` | 2~8개. 세로로 나열 |

`Item` 형식:

| 키 | 타입 | 설명 |
|---|---|---|
| `label` | string | 키 — 모드명·단계명·항목명 (한 줄, 24자 이내 권장) |
| `description` | string? | 한 줄 설명 (60자 이내 권장) |

좌측 accent 컬러 바 + 옅은 틴트의 단일 박스로 렌더된다. 아이콘 없음·순서 번호 없음 — 단순한 강조 리스트가 필요할 때 쓴다.
**언제 안 쓰나:** 순서가 핵심이면 `step-card`, 구성요소 갈래면 `grid-card`, 권장/금지면 `checklist-card`. 본문 흐름에 자연스럽게 녹는 짧은 나열은 그냥 `-` 마크다운으로 둔다.

**`bullet-card` — 한 줄 라벨만 짧게 나열**

| 키 | 타입 | 설명 |
|---|---|---|
| `items` | `string[]` | 2~8개. 한 줄·60자 이내. 설명 없이 라벨만 |

`list-card`와 같은 박스 톤이되, 항목은 accent 색 작은 점 + 한 줄 라벨로 가볍게 떨어진다. "구체적으로는 이런 모습입니다" 다음의 압축 목록처럼, **설명을 굳이 붙일 필요 없는 짧은 나열**에 쓴다.
**언제 안 쓰나:** 키마다 한 줄 설명이 붙으면 `list-card`, 권장/금지로 갈리면 `checklist-card`, 순서가 핵심이면 `step-card`. 본문에서 한 문장으로 충분하면 시각자료를 만들지 않는다.

### ` ```illustration ` — 개념 일러스트 (SVG 직접)

다이어그램·카드로는 안 되는 **개념·은유·구조 스케치**(이해를 돕는 그림)는 ` ```illustration ` 블록에 **SVG를 직접** 그린다.
카탈로그(JSON)와 달리 raw SVG를 그대로 적는다 — 워커가 라이트·다크 두 벌 PNG로 굽는 흐름은 동일하다.

글마다 일러스트가 **한 가족처럼** 보이도록 아래 스타일을 반드시 지킨다.

- **스타일: 플랫 라인 일러스트.**
  면은 옅게 채우고 윤곽은 선으로.
  그라데이션·그림자·사실적 묘사·3D ✗.
- **`viewBox="0 0 800 450"` 고정**(16:9). `width`/`height` 속성은 쓰지 않는다.
- **선 두께 2.5**, `stroke-linecap="round"`·`stroke-linejoin="round"`.
- **색은 디자인 토큰을 `style`로 지정한다.**
  `stroke="..."` 같은 프리젠테이션 속성이 아니라 `style="stroke: var(--fg-strong)"` 형태여야 라이트/다크가 자동 대응된다.
  - 윤곽선: `var(--fg-strong)`, 보조선: `var(--fg-neutral)`
  - 면 채움: `var(--diag-blue-fill)` 등 (위 accent 7색 토큰의 `*-fill`)
  - 강조 윤곽: `var(--diag-blue-stroke)` 등 (`*-stroke`)
- 한 일러스트에 **3색 이내** (메인 + accent 1~2개).
- 첫 자식으로 **`<title>설명</title>`** — 접근성 alt로 쓰인다. 필수.
- SVG 안 글자는 최소화한다.
  구조도·순환도라면 노드마다 짧은 라벨은 괜찮지만(수집·실행 등), 설명 문장은 넣지 않는다 — 설명은 본문이 한다.
- `<script>`·외부 참조·`<image>`(래스터) ✗.
- **흐름 방향을 일관되게.**
  좌→우로 흐르면 끝까지 좌→우로 — 폭이 모자라면 박스를 좁혀 맞추지, 흐름을 아래로 꺾지 않는다.
- **여백·간격·겹침을 의식한다.**
  - 좌·우·상·하에 **30px 이상 안전 여백** — 박스 끝이 viewBox 가장자리(800, 450)에 닿으면 렌더 시 잘려 보인다.
  - 박스와 화살표 사이에 **8~10px 간격** — 화살표 꼬리가 박스에 붙으면 답답해 보인다.
  - 라벨 텍스트는 화살표·곡선과 겹치지 않게 **별도 y 좌표 영역**에 둔다 (예: 곡선 apex가 y=150이면 라벨은 y=180 이하).
- 글당 1개. 정말 그림이 이해를 돕는 곳에만.

````markdown
```illustration
<svg viewBox="0 0 800 450" xmlns="http://www.w3.org/2000/svg">
  <title>요청 하나가 여러 작업으로 갈라지는 모습</title>
  <circle cx="150" cy="225" r="46"
    style="fill: var(--diag-blue-fill); stroke: var(--diag-blue-stroke); stroke-width: 2.5" />
  <path d="M196 225 H360 M360 225 L420 140 M360 225 L420 310"
    style="stroke: var(--fg-neutral); stroke-width: 2.5; fill: none"
    stroke-linecap="round" stroke-linejoin="round" />
  <rect x="420" y="110" width="150" height="60" rx="14"
    style="fill: var(--diag-green-fill); stroke: var(--diag-green-stroke); stroke-width: 2.5" />
  <rect x="420" y="280" width="150" height="60" rx="14"
    style="fill: var(--diag-green-fill); stroke: var(--diag-green-stroke); stroke-width: 2.5" />
</svg>
```
````

### 마크다운 표 — 항목 여럿 × 대상 여럿

대상 2~3개를 여러 항목에 걸쳐 견주는 대조는 **그냥 마크다운 표**로 적는다.
` ```visual ` 카탈로그가 아니라 본문 표이므로, 카드 개수 제한(글당 0~3개)·변주 규칙과 무관하게 필요할 때 쓴다.
카드 대신 표를 기본으로 쓰지 않는 글 유형(`/research` 등)에서도 표는 허용된다.

렌더는 `markdownRender.tsx` 가 맡는다 — `<table>` 을 `.table-scroll` 로 감싸고,
스타일은 `globals.css` 의 `.prose table` 절이 디자인 토큰으로 라이트·다크를 함께 잡는다.
글 쪽에서 따로 HTML·클래스를 적을 필요는 없다.

```markdown
| | Orca | Paseo |
|---|---|---|
| 본체 | 데스크톱 앱 | 데몬(앱은 클라이언트) |
| 원격 | SSH 워크트리 | 데몬을 원격에 두고 붙음 |
| 라이선스 | MIT | Apache-2.0 |
| 별 / 시작 | 67,563 / 2026-03 | 17,080 / 2025-10 |
```

- **첫 칼럼은 그 행의 키**다. 렌더 시 굵게·`fg-strong` 으로 세워지니 `항목 | 값 | 값` 꼴로 짠다.
  첫 칼럼 헤더 칸은 위 예처럼 비워 두는 게 깔끔하다.
- **헤더 행은 반드시 둔다.** 대상 이름(`Orca`·`Paseo`)이 헤더로 올라가야 표가 읽힌다.
- **셀은 한 줄.** 설명이 길어지면 본문 문단으로 내린다 — 표는 견줄 값만 담는다.
- **행 4개 이상일 때 쓴다.** 2~3줄짜리 대조는 `compare-card` 가, 한 줄 대조는 그냥 문장이 낫다.
- 좁은 화면에서는 **표만 가로로 스크롤**되고 페이지는 밀리지 않는다. 칼럼을 억지로 줄이지 않아도 된다.
  다만 대상이 4개를 넘으면 모바일에서 계속 밀어야 하니, 그때는 항목을 추리거나 표를 둘로 나눈다.
- 표 안 값에 `` `코드` `` 를 써도 된다(줄바꿈 없이 한 줄로 떨어진다).
- **표 바로 위 문단이 무엇을 견주는지 한 문장으로 잡아 준다.** 표만 툭 놓지 않는다.

### 아이콘 — Lucide 화이트리스트

아이콘은 [Lucide](https://lucide.dev) 이름을 그대로 적는다(`message-circle`, `search`, `edit`, `check-check`, …).
라이브러리에 없는 이름을 쓰면 워커가 경고하고 기본 점 아이콘으로 폴백한다.
**아래 목록 안에서 고르는 것을 권장**한다 — 카탈로그의 시각적 톤이 일관되게 유지된다.

- 대화·요청: `message-circle`, `message-square`, `send`, `mic`
- 탐색·읽기: `search`, `book-open`, `file-text`, `eye`, `compass`
- 작성·실행: `edit`, `pencil`, `play`, `terminal`, `code`, `rocket`
- 결정·분기: `git-branch`, `split`, `arrow-right-left`, `shuffle`, `scale`, `git-compare`
- 검증·완료: `check`, `check-check`, `circle-check`, `shield-check`, `thumbs-up`
- 경고·실패: `alert-triangle`, `alert-circle`, `x`, `shield-alert`, `ban`
- 데이터·저장: `database`, `hard-drive`, `server`, `cloud`, `archive`
- 사람·조직: `user`, `users`, `building-2`, `briefcase`
- 시간·과정: `clock`, `timer`, `calendar`, `history`, `loader`, `flag`, `milestone`
- 보안·키: `shield`, `lock`, `key`, `fingerprint`
- 측정·지표: `bar-chart-3`, `trending-up`, `trending-down`, `gauge`, `activity`
- 금융·숫자: `dollar-sign`, `coins`, `wallet`, `receipt`
- AI·도구: `bot`, `cpu`, `sparkles`, `zap`, `wrench`, `puzzle`

### 크기 제한 — 작을수록 강하다

- **항목이 3개 이상일 때만 시각자료를 만든다.**
  `step-card`·`stat-card`·`grid-card`·`timeline-card`·`checklist-card`는 나열할 항목이 1~2개면 카드로 키우지 말고 본문 문장으로 적는다 — 2개짜리 카드는 한 줄 문장만 못하다.
  (`callout-card`·`quote-card`는 본래 단일, `compare-card`는 2~3 대상을 견주는 게 목적이라 이 규칙과 무관하다.)
- 한 패턴 안의 요소 수: `step-card` 3~8단계, `stat-card` 3~4지표, `callout-card` 단일, `compare-card` 2~3칼럼, `timeline-card` 3~6사건, `checklist-card` 3~8항목, `grid-card` 3~6타일, `list-card`·`bullet-card` 3~8항목.
- `step-card`·`stat-card`·`grid-card`는 **항목이 한 줄로** 펼쳐진다.
  개수가 많아지면 칸이 한 줄을 지키느라 좁아지고, 모바일처럼 폭이 좁을 때만 여러 줄로 접힌다(에디터 프리뷰에서도 한 줄).
  칸이 너무 좁아 답답하지 않게 **step 3~6 · stat 3~4 · grid 3~5**를 권장한다 — 그보다 많으면 항목을 추리거나 본문 리스트로 옮긴다.
- **글당 0~3개. 기본값은 0.**
  시각자료는 본문 평문만으로 구조·비율·흐름이 그려지지 않을 때만 얹는다.
  "수치가 있으니 한 개는 만들어야지"가 아니라 "이 부분은 카드 없이는 안 잡힌다"가 만드는 이유여야 한다.
  **시각자료가 0개인 글이 절반은 나와도 정상**이다.
- **매번 같은 패턴 조합·같은 위치 금지.**
  직전 글이 `stat-card` + `compare-card` 두 개를 본문 30%·60% 지점에 깔았다면, 이번 글은 시각자료를 빼거나, 다른 패턴(`callout-card`·`timeline-card`·`step-card`·`grid-card` 등)을 다른 지점에 한 개만 쓴다.
  **`stat-card` + `compare-card` 2개 조합 반복이 가장 명백한 자동화 티다.** (2-3절)
- 시각자료 다 채우기보다 **중심 메시지 하나**를 또렷이 — 한 시각자료 = 한 결론.

### 작성 규칙

- ` ```visual ` 블록 안에는 **JSON만** 적는다.
  주석·후행 콤마 ✗ (`JSON.parse`에서 깨진다).
- 라벨·캡션은 **한 줄, 글자 수 권장 안에서.**
  길어지면 본문 문단으로 옮긴다(시각자료는 구조만).
- `accent`는 위 7개 외 값 ✗. 직접 hex/HTML 색 지정 ✗.
- 본문이 이미 설명한 사실을 **요약·구조화**할 때만 쓴다.
  시각자료에서 새 사실·추측을 만들어내지 않는다(원문 보존 원칙).
- 시각자료에 별도 캡션·각주를 달지 않는다.
  바로 위·아래 문단이 맥락을 잡는다.
  필요한 부연은 `eyebrow`·`alt`로 충분하다.
- **원문의 그림·사진·도표는 적극 활용한다.**
  원문에 이해를 돕는 시각자료가 있으면 카탈로그로 새로 그리지 말고 `![설명](원문 이미지 URL)` 마크다운 이미지로 본문에 가져온다.
  원문 이미지 URL을 직접 링크해도 된다.
- **YouTube 글은 소제목별로 영상 장면을 캡처해 넣는 것이 기본이다.**
  `npm run capture`(절차는 `docs/draft-in-claude-code.md`)로 각 소제목 시점의 후보 프레임을 뽑아, 세션이 직접 보고 가장 나은 컷 1장을 고른다.
  슬라이드·자료·핵심 화면처럼 **본문 내용을 뒷받침하는 장면**을 넣는다 — 흐림·전환 중·눈 감음·빈 화면·발표자 표정만 잡힌 컷은 배제하고, 쓸 만한 게 없는 소제목만 건너뛴다.
  영상 글의 기본 시각자료라 카탈로그 카드의 개수 변주(2-3절)와는 별개로 보되, 컷 종류(슬라이드·데모·인물 등)는 매번 똑같이 굳지 않게 다양하게 고른다.
- 카탈로그·이미지로 안 되는 형태는 본문 표·리스트, 또는 직접 HTML/CSS로 표현해도 된다.
  다만 같은 패턴이 반복되면 카탈로그 컴포넌트로 추가하는 편이 디자인 일관성에 낫다(`src/components/visuals/`).

---

## 2-3. 자동화 티 방지 — 글 사이 변주

이 블로그의 초안은 Claude Code 세션이 직접 쓴다.
규약이 상한선만 정해 두면 매 글이 상한선 근처에서 비슷한 형태로 수렴한다.
글 몇 개만 훑어도 "같은 손이 같은 틀로 찍어 냈다"는 인상이 남는다.
이를 피하려면 **개별 글의 규약 준수**보다 **글과 글 사이의 다양성**을 한 단계 위에서 본다고 생각하라.

초안을 쓰기 전에 `drafts/` 안의 최근 글 1~2개를 훑어 다음 항목이 직전 글과 겹치지 않는지 확인한다.

- **제목 형태.**
  직전 글이 `키워드 — 부연구`(em-dash 두 토막)였다면 이번엔 단정 평서·질문·쉼표·수치 앞세움 등 다른 골격으로 (2-0절 "제목 형태를 매번 바꾼다"). 같은 시작 단어·같은 어미도 반복 금지.
- **종결 비율.**
  매 문장 `~습니다.`로 끊지 말고, `~까요/~네요/~거든요` 같은 양념 종결이 한 글에 몇 군데 자연스럽게 박혔는지 본다(`~죠`는 쓰지 않는다).
  반대로 양념이 한 문단에 둘 이상 깔렸으면 가볍다 — 한 글당 2~3개 선에서 줄인다.
- **글 길이.**
  본문 줄 수가 직전 글의 ±15% 안에 들면 의식적으로 늘리거나 줄인다.
- **헤드라인 개수와 호흡.**
  매번 `##` 4개 + 마무리 라벨로 떨어지지 않게 한다. 세 개로 짧게 끝내도 되고, 다섯 개로 길게 가도 된다.
- **도입 첫 문장.**
  "X는 ~입니다" 정형이 두 글 연속이면 이번에는 사실·장면·인용·수치로 시작한다.
- **`<mark>` 위치.**
  직전 글의 형광펜이 글 끝쪽이었다면 이번 글은 형광펜을 빼거나 앞쪽에 둔다.
- **시각자료 개수·패턴 조합.**
  직전 글이 2개였다면 이번은 0~1개.
  직전 조합이 `stat-card` + `compare-card`였다면 이번은 그 조합을 피하고 덜 쓰인 패턴(`grid-card`·`list-card`·`timeline-card`·`quote-card`·`callout-card`)을 시도한다.
- **마무리 저작권 줄.**
  직전 글에 박혔다면 이번 글은 생략한다(기본은 생략).

원칙은 단순하다 — **직전 글과 같은 구조가 보이면 일부러 다른 길을 고른다.**
변주는 규약을 어기는 것이 아니라 규약의 일부다.

---

## 2-4. 퇴고 점검표 — 초안을 다 쓴 뒤 반드시 한 번 더 돈다

규약을 알고 써도 초안에는 어긴 문장이 남는다. 같은 규약으로 쓴 두 글을 견줘 보면, 차이는 규약이 아니라 **점검을 했느냐**에서 났다. 그래서 이 패스는 생략하지 않는다.
사실·구조·링크·시각자료는 그대로 두고 **문장만** 처음부터 다시 읽는다. 아래 항목에 걸리면 그 문장만 다시 쓴다. 검색 가능한 것은 실제로 검색한다.

| # | 찾을 것 | 검색·판별 | 처방 |
|---|---|---|---|
| 1 | `~죠` `~겠죠` 잔존 | `죠.` `죠?` 검색 | `~습니다` 단정, 또는 `~거든요`·`~다고 합니다` |
| 2 | 연결어미 뒤 쉼표 | `고,` `며,` `지만,` `면서,` `서,` 검색 | 쉼표 삭제, 길면 문장을 끊는다 |
| 3 | "X가 아니라 Y" 3회 이상 | `아니라` `아닌` 검색해 센다 | 셋째부터 Y 만 평서로 |
| 4 | 손맛 동사 | `굴리` `뜯어` `걷어` `당겨` `쥐` `짚` `뱉` 검색 (`돌리다`·`띄우다`는 허용) | 표준 동사로(실행·분석·제거·앞당기다…) |
| 5 | 비유 명사·제품 의인화 | 관제탑·자국·경로·자리·물건·눈금·빨간불 류, "X는 ~을 올립니다/세웁니다" | 실제 대상의 이름, 정의문·기능 서술 |
| 6 | 수사 단독 | "다섯을" "수백이" 처럼 단위 없는 수량 | `개`·`대` 등 단위 명사 |
| 7 | 영어 부사·기술 은유 직역 | `조용히` `우아하게` `공격적으로` `단순히` `기본적으로` / `고아` `매달린` `순진한` `신선한` `무거운`(의존) | 정황·뜻을 문장으로 풀거나 영어 원어 그대로. `조용히 실패`는 "아무 표시 없이 실패" |
| 8 | 존재구문·무생물 주어·cleft 도치 | "~가 있습니다. 그 ~는", "흥미로운 건 ~입니다" | 동사로 풀어 한 문장에, 단정 평서로 |
| 9 | 문두 부사+쉼표, 쉼표 삽입절 | "역설적이게도," / "~은, 아무리 ~여도, ~" | 문장 안에서 풀어 쓰거나 한 호흡으로 |
| 10 | 영어 은유 직역·과장 관용구 | "~의 함수", "~의 영토", "쐐기를 박다", "못 박다" | 사실 그대로 적는다 |
| 11 | 한글 용어 뒤 영어 괄호, 인명·제품명 음차 | `(lens)`, "다리오 아모데이" | 괄호 삭제, 영어 원표기 |
| 12 | 매 문장 출처 표딱지·메타 도입 | "Anthropic에 따르면" 반복, "살펴봅시다", "따라갑니다/따라가 보겠습니다", "하나씩 보겠습니다" | 삭제, 사실부터 |
| 13 | 압축 문장 | `셋입니다` `둘입니다` `넷이면` / `정체` `지점` `자리` 로 끝나는 문장 / 한 문장에 `·` 셋 이상 | 풀어서 말하듯 — "세 가지가 있습니다", 명사 마무리는 동사로, 나열은 불릿·표로 |
| 14 | 순화어·비유 동사 | `내려받` `옵니다`(파일이) `출발합니다`(추상) `갈립니다`(추상) | 다운받다·들어있다·"~를 이해하면 ~할 수 있습니다" |
| 15 | 서두 구조 | 첫 문단이 워밍업(장면) → 질문 → 답 → 예시 순인지, `>` 요약이 "아닙니다."류 직답으로 시작하지 않는지 | 장면부터 다시 쓰고, 요약은 예시·얻는 것으로 |
| 16 | 정보 나열체 | `##` 아래 첫 문장이 매번 "X 는 ~입니다" 정의문인지 센다 | 앞 섹션의 궁금증을 받는 질문·연결 문장으로 바꾸고, 왜 그런지 한 줄 덧붙인다 |
| 17 | 시리즈 참조 | `[0-9]편` 검색 | 사실을 한 줄로 다시 적거나 "다음 글에서" 로 |
| 18 | em-dash 헤드라인 | `^## .* — ` 검색 | 한 덩어리 명사구로, `정리 —`·`대책 N —` 접두 제거 |

마지막 판별법: **소리 내어 읽었을 때 한국 개발자가 옆 사람에게 설명하는 말인가.** Papago 가 이렇게 옮기겠는가. 둘 중 하나라도 아니면 고친다.

---

## 3. 출처/참고자료 컨벤션

### 방식 A — 참고 자료 섹션 (기본 권장)

```markdown
## 참고 자료

- 원문: [Project Glasswing — Anthropic](https://www.anthropic.com/glasswing)
- 관련: [관련 글 제목](URL)

> 이 글은 Anthropic의 원문을 번역·요약한 것입니다. 원저작권은 Anthropic에 있습니다.
```

### 방식 B — 각주 (본문 중간 인용 시)

```markdown
Mythos Preview는 CyberGym에서 83.1%를 기록했다[^1].

[^1]: [Project Glasswing, Anthropic](https://www.anthropic.com/glasswing)
```

> ⚠️ 현재 `.prose ul`만 스타일이 있고 `.prose ol` / 각주(`.footnotes`) CSS는 없다 (`src/app/globals.css`).
> **방식 A(불릿)는 그대로 깔끔하게 렌더링**되고, 방식 B(각주)는 동작은 하나 번호·구분선이 밋밋하다.
> 번역·요약 글은 **방식 A를 기본**으로.

---

## 4. 워크드 예시 (미니)

규칙이 한 글에서 어떻게 합쳐지는지 보여 주는 짧은 예시다.
실제 글은 더 길어도 되지만, 톤·구조·분량 감각은 이걸로 잡는다.

**에디터 입력:** 제목만 후킹+검색어로 직접 짓고(2-0절), 태그·카테고리를 고른다.
슬러그·요약·썸네일·읽는 시간은 자동.

- 제목: `프로젝트 글래스윙 — AI가 보안 취약점을 너무 잘 찾는다`
  (앞 = 검색어 "프로젝트 글래스윙", 뒤 = 후킹)

**본문(맨 위는 `>` 요약부터):**

```markdown
> Anthropic이 미공개 AI 모델로 수십 년 묵은 보안 취약점 **수천 개**를 찾아냈습니다. 그런데 이 능력을 혼자 쥐지 않고, 빅테크 11곳과 함께 방어에 먼저 쓰기로 했습니다.

## AI가 27년 묵은 보안 결함을 찾아냈다

사람이 **27년** 동안 못 찾은 OpenBSD 결함을 Anthropic의 미공개 모델이 찾아냈습니다. 자동화 도구를 500만 번 돌려도 안 걸리던 결함이거든요. AI의 취약점 발견 능력이 이미 최정상급 전문가 수준에 올라섰다는 뜻입니다.

## 빅테크 11곳과 함께 방어에 쓰기로 했다

흥미로운 건 그다음입니다. Anthropic은 이 능력을 혼자 쥐는 대신 11개 기업·기관과 공유하기로 했습니다. <mark>공격자가 먼저 손에 넣기 전에 방어자에게 닿게 한다</mark> — 그게 이 프로젝트의 취지라고 합니다.

## 참고 자료

- 원문: [Project Glasswing — Anthropic](https://www.anthropic.com/glasswing)

> 이 글은 Anthropic 원문을 정리한 것입니다. 원저작권은 Anthropic에 있습니다.
```

---

## 부록 — 태그 표기 규약

태그는 자유 입력이라 `AI`/`ai`, `Claude Code`/`claude-code` 처럼 같은 걸 가리키는 표기가
갈라지기 쉽다. 태그 페이지가 사이트맵에 올라간 뒤로는 이게 곧 **중복 콘텐츠**다.
같은 태그가 두 URL 로 색인되고, 둘 다 얇은 페이지로 평가받는다.

**판정 기준은 `tagKey`** — 소문자화 + 공백·언더스코어를 하이픈으로. 이 키가 같으면 같은 태그다.
표기 자체를 슬러그로 강제하지는 **않는다**. 한글 태그(`AI 안전` → `ai-안전`)가 어색해지기 때문이다.

동작하는 지점:

| 어디 | 무엇을 하나 |
|---|---|
| 어드민 저장 / `/draft` / AI 워커 | 입력 태그를 이미 쓰이는 표기로 정렬(`canonicalizeTags`). 새 태그는 입력 표기 유지 |
| `/tags/<옛표기>` | 정규 표기로 **301 영구 이동**. 옛 주소가 빈 페이지로 남지 않는다 |
| `npm run tags:normalize` | DB 전체 표기 통합. 인자 없으면 dry-run, `-- --apply` 로 반영 |

대표 표기를 고르는 규칙은 **많이 쓰인 표기 > 슬러그형 > 사전순**(`pickCanonical`).

원본은 `src/lib/tags.ts`, 스크립트용 사본은 `scripts/lib/tags.mjs`.
**한쪽만 고치면 어긋난다** — `src/lib/tags.test.ts` 가 두 구현을 대조해 잡는다.
